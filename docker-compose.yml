version: "3.8"

services:
  worker:
    build:
      context: .
      dockerfile: worker/Dockerfile
    env_file:
      - .env.local
    volumes:
      - worker-tmp:/tmp/skillbench
    restart: unless-stopped

volumes:
  worker-tmp:
